<div class="container">
	<div class="positioning">
		<div class="form-style">
			<h1>Add a Booking</h1>
			<%= simple_form_for [@surf_company, @booking] do |f| %>
			  <%= f.input :date %>
			  <%= f.input :time %>
			  <div class="button">
			  	<%= f.button :submit, class: "btn-primary" %>
			  </div>
			<% end %>
		</div>
	</div>
</div>
